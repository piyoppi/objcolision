export default class colisionForceControl{
    constructor(){
        this._default_k = 0.1;                   //めり込み回避ばね定数
    }

    changeForce(items){
        items.forEach( item => {
            //ばね力
        });
    }
}
