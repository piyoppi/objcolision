version: '3'
services:
  gameapp:
    build: .
    command: /sbin/init
    privileged: true
    hostname: app.local
    container_name: gameapp
    ports:
      - '3000:3030'
    volumes:
      - ./:/var/www/

    #command: bash -c "cd /var/www && bundle exec rails s"
        #command: /sbin/init

  gamedb:
    image: mysql/mysql-server:5.7
    container_name: gamedb
    environment:
      - MYSQL_ROOT_PASSWORD="hoge"
